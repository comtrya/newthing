apiVersion: v1
kind: Service
metadata:
  name: comtrya-server
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: comtrya-server
  ports:
    - name: http
      port: 80
      targetPort: http
      protocol: TCP
