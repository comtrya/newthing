apiVersion: v1
kind: ServiceAccount
metadata:
  name: comtrya-server
# The server needs no Kubernetes API access; the dedicated SA exists so its
# token isn't the namespace default and to anchor future RBAC if needed.
automountServiceAccountToken: false
