[package]
name = "forgepoint-server"
version.workspace = true
edition.workspace = true

[dependencies]
axum = "0.7.9"
forgepoint-core = { path = "../core" }
serde = { version = "1.0.228", features = ["derive"] }
serde_json = "1.0.149"
tokio = { version = "1.52.1", features = ["macros", "net", "rt-multi-thread"] }
